import { Input, Card, CardBody, CardHeader, Button, Form } from "@heroui/react"
import { useState } from "react"


export default function Homepage(){
    return (
        <main className="w-full dark flex flex-col items-center justify-center h-screen text-foreground bg-background">
            <div  className="h-1/2 absolute w-full"/>
            <div />

        </main>
    )
}